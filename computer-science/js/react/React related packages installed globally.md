# React related packages installed globally

`npm install -g @babel/core @babel/node`

## Can watch now

`babel source.js -o output.js`

`npm install -g @babel/preset-env @babel/preset-react`

## Set .babelrc

`{`
`    "presets": [`
`        "@babel/preset-env",`
`        "@babel/preset-react"`
`    ]`
`}`

